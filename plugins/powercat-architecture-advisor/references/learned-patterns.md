# Learned Patterns — Power CAT Architecture Advisor

This file is appended to automatically when the skill encounters a scenario domain that
does not match any entry in the main schema hints table in SKILL.md.

Entries here are reviewed by skill maintainers and promoted to the main table when validated.

## Format

Each row uses the same column format as the main schema hints table:

| Industry / Domain                       | Keywords to match | Suggested tables / patterns | Compliance / integration flags |
|-----------------------------------------|-------------------|-----------------------------|--------------------------------|
| _(new entries appended below this row)_ |                   |                             |                                |
